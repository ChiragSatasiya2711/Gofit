import 'package:flutter/material.dart';

class TabbarComman extends StatelessWidget {
  const TabbarComman({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return TabBar(
      tabs: List.empty(),
    );
  }
}
